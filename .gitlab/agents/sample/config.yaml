gitops:
  manifest_projects:
  - id: 55934672
    ref:
      branch: master
  default_namespace: robot-shop
  paths:

  reconcile_timeout: 3600s
  dry_run_strategy: none
  prune: true
  prune_timeout: 3600s
  prune_propagation_policy: foreground
  inventory_policy: must_match
  
